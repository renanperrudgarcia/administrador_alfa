<%- contentFor('body')%>
<div class="alert alert-primary" role="alert">
    <h4>Usuários</h4>
</div>

<table class="table table-striped">
    <thead>
        <tr>
            <th>Nome</th>
            <th>Login</th>
            <th>Email</th>
            <th> </th>
            <th> </th>
        </tr>
    </thead>
    <tbody>
        <% for(d of data) {%>
        <tr>
            <th><%- d.nome %></th>
            <td><%- d.login %></td>
            <td><%- d.email %><td>
            <td>
                <a href="/edit_usuarios" class="btn btn-warning rounded-circle">
                    <span class="glyphicon glyphicon-pencil" aria-hidden="true"></span>
                </a>
            </td>
            <td>
                <button class="btn btn-danger rounded-circle" type="button">
                    <span class="glyphicon glyphicon-trash" aria-hidden="true"></span>
                </button>
            </td>
        </tr>
        <% } %>
    </tbody>
</table>
<div class="fixed-bottom">
    <a href="/add_usuarios" class="btn btn-primary float-right rounded-circle" style="margin-bottom: 2em; margin-right: 2em;">
        <span class="glyphicon glyphicon-plus" aria-hidden="true"></span>
    </a>
</div>